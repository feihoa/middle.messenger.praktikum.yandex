<section class="chats-list-section">
  {{{ Button label="+" type="text" class="next add-btn" onClick=onAddChat }}}
  {{{ Link label="Профиль" page="/settings" class="upper" }}}
  <div class="form-container">
    {{#> Form formLayout="horizontal"}}
      {{{ InputField label="" placeholder="Поиск" type="text" ref="search" name="search" onEnter=onSearch validate=validate.search }}}
      <div class="form-container__button">
        {{{ Button label="🡆" type="submit" class="next" onClick=onSearch }}}
      </div>
    {{/Form}}
  </div>
  {{{ ChatList chats=chats onClick=onChatClick currentChatId=currentChatId }}}
</section>
